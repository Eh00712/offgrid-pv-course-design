# AGENTS.md — Agent 协作规范（强制）

> 本文件是**项目协作的总规则**。任何 AI agent（Claude Code、Codex、Cursor、Copilot、DSH 等）在参与本仓库工作时，**必须先读取并遵守本文件**。所有 agent 工具打开本仓库时都应自动读取本文件。

## 1. 项目一句话

户用离网光伏发电系统 · 2026 专业课程创新研修课程设计，多人（含多个 AI agent）协作。

## 2. 每次会话的强制工作流

任何 agent 开始工作前和结束后，必须按以下顺序执行：

```
1. git pull                        # 拉取最新，避免冲突
2. 读 docs/PROJECT_LOG.md          # 看最近日志，了解别人/别的 agent 做了什么
3. 读 docs/PROGRESS.md             # 看任务进度表，找到自己负责的任务
4. 执行任务（只改自己负责的部分）
5. 更新 docs/PROJECT_LOG.md        # 追加一条日志（见 §3 格式）
6. 更新 docs/PROGRESS.md           # 完成的任务改状态（见 §4 格式）
7. git add -A && git commit        # 按 §5 提交信息规范
8. git push                        # 立即推送，不留本地独有改动
```

**规则：没有更新日志/进度，就不算完成一次工作。** 宁可日志啰嗦，不可缺失。

## 3. 项目日志标准（docs/PROJECT_LOG.md）

每次会话（无论成果大小）结束时，在 `docs/PROJECT_LOG.md` **顶部**插入一条新记录（最新在上），格式严格如下：

```markdown
## YYYY-MM-DD HH:MM — <提交信息前几个字>
- **执行者**: <agent 名/成员名>（GitHub 用户名）
- **目标**: 本次会话要解决的问题
- **完成内容**: 做了什么（分点列出，写清改了什么、为什么）
- **变更文件**: 新增/修改/删除的文件清单
- **提交**: <commit hash 前 7 位>（如有多个提交，写最后一个）
- **遗留问题**: 未完成或需要注意的事（没有则写“无”）
- **下一步**: 建议下一个 agent/成员做的事
```

示例：

```markdown
## 2026-08-31 17:10 — feat(docs): 添加项目日志与进度标准
- **执行者**: agent-01（sunny1043）
- **目标**: 为多 agent 协作建立日志与进度规范
- **完成内容**: 新增 AGENTS.md 规范；新增日志/进度模板
- **变更文件**: AGENTS.md、docs/PROJECT_LOG.md、docs/PROGRESS.md、README.md
- **提交**: fda84fe
- **遗留问题**: 无
- **下一步**: 各成员认领任务并开始设计
```

## 4. 上传进度标准（docs/PROGRESS.md）

进度表是一个 Markdown 表格，每行一个任务，状态只允许：`🔲 未开始 / 🟡 进行中 / ✅ 已完成`。

```markdown
| 任务 | 负责人 | 状态 | 开始日期 | 完成日期 | 相关提交 |
|------|--------|------|----------|----------|----------|
| 任务1 设计说明书 | agent-01 | 🟡 进行中 | 2026-08-31 | - | - |
| 任务2 系统拓扑图 | agent-02 | 🔲 未开始 | - | - | - |
```

规则：
- 开始做某任务前，先把状态从 `🔲` 改为 `🟡` 并填开始日期（一次提交）；
- 完成后改 `✅` 填完成日期和相关提交 hash（同一次提交内完成）；
- 一个任务一次只能有一个负责人，避免两个 agent 同时改同一任务。

## 5. 提交信息规范（上传标准）

格式：`<类型>(<范围>): <一句话描述>`

| 类型 | 用途 | 示例 |
|------|------|------|
| `feat` | 新增内容/功能 | `feat(docs): 添加光伏组件选型表` |
| `fix` | 修正错误 | `fix(docx): 修正任务书第三章数据` |
| `docs` | 只改文档 | `docs: 更新 README 协作指南` |
| `log` | **只**更新日志或进度表 | `log: 记录 agent-02 进度` |
| `chore` | 杂项/整理 | `chore: 清理临时文件` |

要求：
- 必须用中文描述，**写清「改了什么」**，不要写 `update`、`修改` 这种空泛信息；
- **纯日志更新**用 `log:` 类型——同伴只看 `git log --oneline | grep log` 就能知道全队进度；
- 一次提交只做一件事（一个文件类别的改动），不要把日志和功能混在一起提交。

## 6. 推送与冲突规则

- 每次会话结束**必须 push**，禁止在本地堆积多个未推送提交；
- 推送前若 `git push` 被拒绝，执行 `git pull --rebase` 后重新 push；有冲突时**保留双方内容**，绝不覆盖别人成果；
- 冲突解决后，在日志里注明「解决了与谁的冲突」。

## 7. 禁止事项

- ❌ 不读 AGENTS.md、不更新日志就提交；
- ❌ 提交信息空泛（`update` / `修改` / `fix`）；
- ❌ 覆盖/删除他人负责的文件而不在日志中说明；
- ❌ 推送未经验证的损坏文档（docx/doc 无法打开）；
- ❌ 把个人信息（学号、密码、隐私）传进仓库。
